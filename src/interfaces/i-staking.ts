/* eslint-disable semi */
/* eslint-disable unicorn/prevent-abbreviations */

export default interface IStaking {
  id: string;
  name: string;
  provider: string;
  quantity: number;
  apr: number;
  apy: number;
}
