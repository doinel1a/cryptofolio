/* eslint-disable semi */
/* eslint-disable unicorn/prevent-abbreviations */

export default interface ITokenData {
  id: number;
  name: string;
  symbol: string;
  logoURL: string;
  currentPrice: number;
}
