enum EErrorMessages {
  empty = 'must not be empty',
  invalidString = 'must only contain letters',
  invalidNumber = 'must be a valid number',
  invalidDate = 'must be a valid date'
}

export default EErrorMessages;
